# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/plutodb

# Redis (optional – caching for ChainLens, Trendcraft, PulseBot). If unset, caching is skipped.
# REDIS_URL=redis://localhost:6379

# Server port (default: 5000)
PORT=5000

# JWT (email/password auth)
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Google OAuth (sign-in with Google): https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application), add authorized JS origins and redirect URIs for your frontend
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# Required for Gmail/email integration (server-side OAuth). Same Google Cloud project, create OAuth client with secret.
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Twitter (quest verification)
TWITTER_BEARER_TOKEN=your-twitter-api-v2-bearer-token
RAPIDAPI_KEY=your-rapidapi-key

# Moralis (ChainLens wallet intelligence)
MORALIS_API_KEY=your-moralis-api-key

# TrendCraft (aggregated trends: YouTube, Reddit, Hacker News, News)
# YouTube: https://console.cloud.google.com/apis/library/youtube.googleapis.com
YOUTUBE_API_KEY=your-google-cloud-youtube-data-api-key
# NewsAPI: https://newsapi.org/register (free tier: dev/localhost only)
NEWS_API_KEY=your-newsapi-org-key
# Reddit: no key required; descriptive User-Agent required
REDDIT_USER_AGENT=TrendCraft/1.0
# Gemini (content generation & suggestions): https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# PulseBot (Telegram bot for chat context & summaries): from @BotFather (t.me/Pluto_PulseBot)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# Public URL of this backend (required for /link to work). For local dev use ngrok: ngrok http 5000, then set to https://xxx.ngrok.io
# Then open in browser: https://xxx.ngrok.io/api/pulsebot/set-webhook to register the webhook.
PULSEBOT_WEBHOOK_BASE_URL=
# For Gmail OAuth callback: public backend URL (e.g. https://xxx.ngrok.io or https://api.yourapp.com)
# BACKEND_PUBLIC_URL=https://xxx.ngrok.io
# Where to redirect after Gmail connect (defaults to CORS_ORIGIN)
# FRONTEND_ORIGIN=http://localhost:3000

# Production: set NODE_ENV=production (disables x-user-id auth bypass)
# CORS: restrict origins in production (e.g. CORS_ORIGIN=https://yourapp.com)
CORS_ORIGIN=*
