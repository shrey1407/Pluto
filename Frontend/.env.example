# Pluto Frontend — copy to .env and fill in values
# API base URL (Pluto backend)
VITE_API_BASE_URL=http://localhost:5000

# Google OAuth 2.0 Client ID (Web application) — same as Backend GOOGLE_CLIENT_ID
# https://console.cloud.google.com/apis/credentials
# In the OAuth client add:
#   Authorized JavaScript origins: http://localhost:3000 (and your production URL)
#   Authorized redirect URIs:     http://localhost:3000/login/callback (and https://yourdomain.com/login/callback for prod)
VITE_GOOGLE_CLIENT_ID=

# WalletConnect project ID (RainbowKit) — https://cloud.walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=your_project_id
